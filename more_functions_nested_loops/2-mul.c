#include "main.h"
#include <stdio.h>

/**
 * mul - function that multiplies two integers
 * @a: character value
 * @b: value
 * Return: Always  multiplications of a*b
 */

int mul(int a, int b)
{
	int p;

	p = (a * b);
	return (p);
}
