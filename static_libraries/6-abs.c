#include "main.h"

/**
 * _abs - writes the character c to stdout
 * @x:The character to print
 *
 * Return: On success 1.
 *
 */

int _abs(int x)
{
	if (x >= 0)
	{
	return (x);
	}
	else
	{
	return (-1 * x);
	}
}
