#include <stdio.h>
/**
 * main-hold all the code
 *
 * Return: Always 0
 */

int main(void)
{
int x;
int y;
for (x = 0; x <= 9; x++)
{
putchar(x);
}
for (y = 'a'; y <= 'f'; y++)
{
putchar(y);
}
putchar('\n');
return (0);
}
